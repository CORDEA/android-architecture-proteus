apply plugin: 'java-library'

apply plugin: 'kotlin'

dependencies {
    implementation Deps.kotlin_stdlib
}

compileKotlin {
    kotlinOptions {
        freeCompilerArgs = ["-XXLanguage:+InlineClasses"]
    }
}
