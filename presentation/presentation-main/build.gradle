apply plugin: 'java-library'

apply plugin: 'kotlin'

dependencies {
    api project(":presentation:presentation-shared")
    implementation project(":di:di-scope")
    implementation Deps.kotlin_stdlib
}
